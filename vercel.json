{
  "version": 2,
  "buildCommand": "pnpm --filter @md-ops/api build && NEXT_PUBLIC_API_URL=/api NEXT_PUBLIC_BASE_PATH= STATIC_EXPORT=true pnpm --filter @md-ops/web build",
  "outputDirectory": "apps/web/out",
  "rewrites": [
    {
      "source": "/uploads/:path*",
      "destination": "/api/uploads/:path*"
    },
    {
      "source": "/health",
      "destination": "/api/health"
    }
  ]
}
