{
  "version": 2,
  "buildCommand": "cd apps/api && npm install && npm run build",
  "outputDirectory": "apps/api/dist",
  "roots": [
    {
      "src": "apps/api",
      "use": "@vercel/node"
    }
  ]
}
