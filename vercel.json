{
  "version": 2,
  "buildCommand": "pnpm --filter @md-ops/api build",
  "rewrites": [
    {
      "source": "/uploads/:path*",
      "destination": "/api/index?path=/uploads/:path*"
    },
    {
      "source": "/health",
      "destination": "/api/health"
    }
  ]
}
