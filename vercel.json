{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && pnpm install",
  "buildCommand": "pnpm --filter @md-ops/shared build && pnpm --filter @md-ops/api build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
